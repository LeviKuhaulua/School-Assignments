# HTTP Server with C

Program:

- Binds a server socket
- Client connects to server socket
- Server sends some message like `Connection Established. Server creator: Levi`
- close connection to client
- Server continues to run 